This project was built using the concept of WebSocket which is implemented by Spring Boot using STOMP protocol and SockJS.

WebSocket is protocol that sits on top of TCP.Websocke establishes a full duplex connnection between client and server whenever a client sends a handshake request and keeps the connection live for instant messaging.


